import SwiftUI

/// Placeholder stub — components agent implements per SPEC-V2 §3:
/// group data grid — member rows × 7 day-columns of 5 mini squares,
/// GridCellState colors per §2; legend included.
struct WeekGridView: View {
    let rows: [MemberWeekRow]

    var body: some View {
        EmptyView()
    }
}
