import SwiftUI

/// Placeholder stub — components agent implements per SPEC-V2 §3.
struct PhotoSquare: View {
    let entry: GridEntry
    let size: CGFloat

    var body: some View {
        EmptyView()
    }
}
