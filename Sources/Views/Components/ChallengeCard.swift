import SwiftUI

/// Placeholder stub — components agent implements per SPEC-V2 §3:
/// emoji, title, progress bar current/target, reward chip, done state.
struct ChallengeCard: View {
    let progress: ChallengeProgress

    var body: some View {
        EmptyView()
    }
}
