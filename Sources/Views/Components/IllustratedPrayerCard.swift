import SwiftUI

/// Placeholder stub — components agent implements per SPEC-V2 §3:
/// deterministic cozy prayer-space illustration (mat, rug patterns,
/// window/lamp variants) from seeded values.
struct IllustratedPrayerCard: View {
    let seed: UInt64

    var body: some View {
        EmptyView()
    }
}
