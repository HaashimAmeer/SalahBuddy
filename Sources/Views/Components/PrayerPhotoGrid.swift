import SwiftUI

/// Placeholder stub — components agent implements per SPEC-V2 §3:
/// 2-col grid; waiting = dashed square w/ avatar emoji; posted shows
/// image/illustration + name + timestamp + tier dot; qada = blue "Made up"
/// tile; missed = mist tile; excused = lilac moon tile.
struct PrayerPhotoGrid: View {
    let entries: [GridEntry]
    let compact: Bool

    var body: some View {
        EmptyView()
    }
}
